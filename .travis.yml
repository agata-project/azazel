language:
  - python

python:
  - "3.6"

sudo: required

install:
  - apt install python3-pip
  - python3 -m pip install pytest
  - python3 -m pip install python-coveralls
  - python3 -m pip install pytest-cov

before_scrip:
  - py.test tests/ --cov=azazel

script:
  - py.test -v --cov azazel --cov-report term-missing

after_success:
  - coveralls
